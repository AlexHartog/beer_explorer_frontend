// import { User } from "./user"


export interface Points {
    user_name: string
    total_points: number
    points_unique: number
    points_bars: number
    points_weeks: number
    points_joint_checkin: number
}